# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment — never commit secrets
.env
.env.local
.env.production

# Cloudflare Worker local dev
.dev.vars
.wrangler

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Project reference docs — not part of the deployed site
docs/

# Skill installs — not part of this project's own source; .claude/hooks,
# .claude/launch.json, and .claude/settings.json stay tracked, just the
# bulky third-party skill definitions under .claude/skills are excluded
.claude/skills/
.agents/
